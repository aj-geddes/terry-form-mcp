apiVersion: v1
kind: Service
metadata:
  name: {{ include "terry-form-mcp.fullname" . }}
  labels:
    {{- include "terry-form-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: mcp
      protocol: TCP
      name: http
  selector:
    {{- include "terry-form-mcp.selectorLabels" . | nindent 4 }}
