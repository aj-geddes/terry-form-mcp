apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "terry-form-mcp.fullname" . }}-test-connection"
  labels:
    {{- include "terry-form-mcp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "terry-form-mcp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
